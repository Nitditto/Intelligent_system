# Customer-behaviour application (Olist) — Python dependencies (notebook + API).
# The web client is React + Vite (see web/package.json); the mobile client is
# Flutter (see mobile/pubspec.yaml). Tested on Python 3.13.
#
# Exact versions used to train the persisted pipeline are printed in notebook
# section 0 and recorded in model/input_schema.json (sklearn 1.9.0, seed 42).

# --- core ML / data ---
numpy>=2.0
pandas>=2.2
scikit-learn>=1.5
scipy>=1.13
joblib>=1.3

# --- notebook / EDA ---
matplotlib>=3.8
jupyter>=1.0
nbclient>=0.10
ipykernel>=6.29

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# --- API smoke tests / scripts ---
requests>=2.31

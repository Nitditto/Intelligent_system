# diabetes — Python dependencies (notebook only, no API this round)
# No TensorFlow / PyTorch / Keras: the deep learning model is implemented
# from scratch with NumPy, per REQUIREMENT.md's explicit constraint.

numpy>=2.0
pandas>=2.2
scikit-learn>=1.5
scipy>=1.13
joblib>=1.3

matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0
nbconvert>=7.0
